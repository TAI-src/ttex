from distutils.core import setup

setup(
    name="ttex",
    version="0.1",
    packages=[
        "ttex",
    ],
    install_requires=[],
    license="GPL3",
    long_description="Tool for experiments",
)
